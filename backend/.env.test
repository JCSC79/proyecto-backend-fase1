# Test environment variables — dummy credentials for unit tests.
# The DAOs are mocked in all tests so no real DB connection is ever made.
NODE_ENV=test
DB_PASSWORD=test_not_used
DB_HOST=localhost
DB_USER=postgres
DB_NAME=tasks_db_test
RABBITMQ_URL=amqp://localhost
JWT_SECRET=test_secret_key_for_unit_tests_32chars
